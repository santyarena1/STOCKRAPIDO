{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "NODE_ENV=development npm install --include=dev --legacy-peer-deps",
  "buildCommand": "NODE_ENV=development npm install --include=dev --legacy-peer-deps && npx prisma@6.19.2 migrate deploy && npx prisma@6.19.2 generate && npx nest build && mkdir -p public && echo \"StockRapido API\" > public/index.html",
  "outputDirectory": "public",
  "functions": {
    "api/index.js": {
      "includeFiles": "dist/**",
      "maxDuration": 120
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ],
  "crons": [
    { "path": "/sync/cron", "schedule": "0 6 * * *" }
  ]
}
